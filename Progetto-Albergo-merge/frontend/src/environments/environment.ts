export const environment = {
  production: false,
  // progettoalbergo espone le rotte alla radice (es. /utente, /camera, /auth), niente prefisso /api
  // apiUrl: 'http://localhost:8080'

  //modificato per errore cross origin
  apiUrl: '/api' //  MODIFICATO: Sostituisci http://localhost:8080 con /api
};


